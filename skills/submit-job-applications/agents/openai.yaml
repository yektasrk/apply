interface:
  display_name: "Submit Job Applications"
  short_description: "Run six visible job-application workers"
  default_prompt: "Use $submit-job-applications to run the default rolling pool of up to six visible external-Chrome workers, keep each job at its review or blocker gate, verify approved submissions, record each outcome in the sheet, and only then recycle that worker slot."
